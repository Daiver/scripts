/*#include <vector>
#include <string>
using namespace std;

class button {
      string text;
      float timer;
      int x,y;
      public:
             int state;
             void Create(string in_text);
             void Render(int in_x,int in_y);
};*/

#include "d.h"

class dialog {
    button buttons[2] ;
};
int main(int argc, char** argv)
{
    return 0;
}
