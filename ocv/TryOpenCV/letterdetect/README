Простенькая распознавалка символов
В текущей версии работает на искусственном примере

Скрипт берет изображение, пытается выделить символы, обучает некое подобие нейросети первым 10 символам, и пытается распознать все символы на изображении

Выделение символов происходит в 2 этапа - сначала находим строки (области на изображении которые можно заключить в две полосы белых пикселей), а затем
в них таким же образом ищутся символы

Отсюда следует, что текст должен быть в горизонтальных строках (или в строках с небольшим наклоном), иначе ничерта не найдет :)
Еще символы которые состоят более чем из одной "фигуры" (типа двойных кавычек, буквы Ы и т.д.) выделяются по частям

Нейросеть взята из репозитория https://github.com/Daiver/SimplePerceptron (только немного изменены изначальные веса), сам алгоритм взят из соответствующей статьи с Хабра

Детектирует символы с переменным успехом, как ни странно при большом обучении эффективность падает :(

Для работы требуется интерпретатор Python и  OpenCV к нему (за счет нее я не совсем тривиально обрабатываю изначальное изображение)

