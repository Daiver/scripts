#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "log.h"

void addLog(const char *str)
{
/*
    FILE *output;

    if((output=fopen(LOG_PATH, "a+"))==0)
    {
        puts ("Can't open output file.");
        exit(-1);
    }
    fprintf(output, "%s\n", str);
    fclose(output);
*/
}
